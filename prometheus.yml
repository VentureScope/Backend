global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: venturescope_backend
    static_configs:
      - targets: ['backend:8000']
    scrape_interval: 15s
    metrics_path: /metrics
